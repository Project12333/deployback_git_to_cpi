To find the value of `edge_cost` in the object `costs`, you need to locate where `"edge_cost"` is assigned.

Looking through the keys, I see that:

```javascript
{
  "edge_cost": 39214804,
  ... // other keys and values...
}
```

The value of `edge_cost` is 39214804.

So the final answer is:

\boxed{39214804}